(ns oplenario.motor.db.calendario)

;; funcoes sobre datasource — 'motor.calendario_feriado' (B4 §22.7.6). Lido por proximo_dia_util/soma_dias_uteis.
;; municipio_id cruza por guard p/ cadastros.municipios (sem FK cross-schema). §22.4.4 deferida.
