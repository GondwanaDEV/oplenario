(ns oplenario.motor.db.registry-versao)

;; funcoes sobre datasource — 'motor.registry_catalogo_versao' (B3 §22.7.6). Da referente ao registry_versao_ref
;; carimbado em template_compliance e dirige o passe de re-validacao no deploy quando uma assinatura muda. §22.4.4 deferida.
