(ns oplenario.motor.db.template-compliance)

;; funcoes sobre datasource — 'motor.template_compliance' (definicao da regra, B1 §22.7.6). HoneySQL
;; schema-qualified, NAO e port. Versionada por copia integral; INVALIDA nunca vira 'vigente'. Persistencia concreta deferida (§22.4.4).
