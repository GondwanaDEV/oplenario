(ns oplenario.legislativo.diplomat.http.out)

;; outbound: HTTP client que IMPLEMENTA os port (chama outros módulos)

;; protocolo de SAIDA p/ Cadastros co-localizado (defprotocol + record HTTP client). Era port/cadastros.
