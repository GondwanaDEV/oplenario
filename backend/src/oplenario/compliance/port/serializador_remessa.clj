(ns oplenario.compliance.port.serializador-remessa)

;; protocolo de SAIDA: SerializadorRemessa (§22.7.8 D4). 1 adapter (SIM); generaliza no 2o TCE (S2).
;; Renderiza o descritor declarativo de layout (dado, dec. 2b) — NAO estende a DSL de avaliacao.
