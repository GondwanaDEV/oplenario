(ns oplenario.compliance.port.fontes)

;; protocolos de SAIDA: read-ports EM LOTE p/ proveniencia (§22.7.8 D7) — cadastros/transparencia/legislativo/sessoes.
;; impl = HTTP client em components; NUNCA JOIN cross-schema (§22.10).
