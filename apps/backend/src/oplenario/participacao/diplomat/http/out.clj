(ns oplenario.participacao.diplomat.http.out)

;; outbound: HTTP client que IMPLEMENTA read-ports cross-modulo (ex.: revogar-consentimento do titular via
;; guard do host sobre repo-identidade, F6 Slice 4). Stub em Slice 1 (segura a silhueta ADR-0001).
