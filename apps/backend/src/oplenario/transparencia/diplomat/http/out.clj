(ns oplenario.transparencia.diplomat.http.out)

;; outbound: nenhuma dependencia cross-modulo sincrona nesta fatia (o portal projeta so' de eventos). Stub p/
;; a silhueta ADR-0001 (mesmo padrao de participacao/diplomat/http/out.clj).
