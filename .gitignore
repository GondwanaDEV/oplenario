# macOS
.DS_Store

# Clojure / JVM
target/
.cpcache/
.lsp/
.clj-kondo/.cache/
.nrepl-port
*.class

# Node / Next.js (frontend)
node_modules/
.next/
# `out/` (export estatico do Next) ANCORADO ao app — a regra generica engolia os dirs-fonte adapters/out e
# wire/out da silhueta ADR-0001 do backend (modulo nao compilava num clone limpo).
apps/*/out/
dist/

# Env / segredos
.env
.env.*
!.env.example

# Editores
.idea/
.vscode/
*.iml

# artefatos de screenshot/preview (design)
.playwright-mcp/
# a raiz não guarda PNG de fonte — só previews descartáveis de validação
/*.png

# companheiro visual de brainstorming (superpowers) — mockups de sessão, não é produto
.superpowers/

# saída do graphify (ferramenta de knowledge-graph local) — não é produto
graphify-out/
